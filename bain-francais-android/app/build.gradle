plugins { id 'com.android.application' }

android {
    namespace 'com.appmada.bainfrancais'
    compileSdk 35

    defaultConfig {
        applicationId 'com.appmada.bainfrancais'
        minSdk 24
        targetSdk 35
        versionCode 3
        versionName '1.2.0'
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
